#include<iostream>
#include<string>
#include<cmath>
#include <algorithm>
#include <iomanip>
using namespace std;

int main() {
	long long x, y;
	cin >> x >> y;
	cout << x << " + " << y << " = " << x + y << "\n"
		<< x << " * " << y << " = " << x * y << "\n"
		<< x << " - " << y << " = " << x - y << "\n";

}